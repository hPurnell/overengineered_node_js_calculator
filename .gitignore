node_modules/
dist/
.next/
out/
coverage/
*.tsbuildinfo

# Local database artifacts
*.db
*.db-journal
*.db-wal
*.db-shm
data/

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/*
# Shared editor config is committed on purpose: debug profiles and build tasks
# are part of the project's setup, not personal preference. Everything else
# under .vscode (settings.json, per-user state) stays ignored.
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
